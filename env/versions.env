# Single source of truth for all component versions.
# Parsed by Makefile and passed to docker build via --build-arg.
# Update this file to upgrade the entire stack.

CUDA_VERSION=12.4.1
PYTHON_VERSION=3.12.10
TORCH_VERSION=2.6.0
TRITON_VERSION=3.2.0
UBUNTU_VERSION=22.04
